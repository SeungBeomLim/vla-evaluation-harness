# Goal-image IDM cache config for LIBERO + GR00T preference pairs.

paths:
  pairs: train/outputs/groot_libero_chunk_preference_pairs_260508/preference_pairs.jsonl
  checkpoint: train/idm/runs/idm_goal_image_libero_groot_h3_260526/best.pt
  output_dir: train/idm/outputs/groot_libero_chunk_preference_pairs_goal_image_idm_h3_260526

data:
  preset: libero:groot
  image_keys:
    - image_agentview
    - image_wrist
  state_key: state

cache:
  clamp: true

runtime:
  device: auto
  cache_size: 32
